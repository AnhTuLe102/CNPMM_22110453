<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CRUD Form</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body>
    <div class="container mt-5">
        <h2 class="mb-4">
            <%= typeof user !=='undefined' && user ? 'Edit User' : 'Create User' %>
        </h2>

        <form action="<%= typeof user !== 'undefined' && user ? `/update-crud?id=${user._id}` : '/api/users' %>"
            method="POST">
            <div class="form-row">
                <!-- First Name -->
                <div class="form-group col-md-6">
                    <label for="firstName">First Name</label>
                    <input type="text" class="form-control" id="firstName" name="firstName"
                        value="<%= typeof user !== 'undefined' && user ? user.firstName : '' %>"
                        placeholder="Enter first name" required>
                </div>

                <!-- Last Name -->
                <div class="form-group col-md-6">
                    <label for="lastName">Last Name</label>
                    <input type="text" class="form-control" id="lastName" name="lastName"
                        value="<%= typeof user !== 'undefined' && user ? user.lastName : '' %>"
                        placeholder="Enter last name" required>
                </div>
            </div>

            <div class="form-row">
                <!-- Email -->
                <div class="form-group col-md-12">
                    <label for="email">Email</label>
                    <input type="email" class="form-control" id="email" name="email"
                        value="<%= typeof user !== 'undefined' && user ? user.email : '' %>" placeholder="Enter email"
                        required>
                </div>
            </div>

            <button type="submit" class="btn btn-primary">
                <%= typeof user !=='undefined' && user ? 'Update User' : 'Create User' %>
            </button>
            <a href="/users" class="btn btn-secondary ml-2">Back to List</a>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>